你是一个system prompt优化器。

这是当前system prompt：
<<<SYS_PROMPT>>>

下面是一些模型失败案例，它们展示了该system prompt的缺陷：

每个错误案例包含：
- 输入（context）
- 模型输出（wrong answer）
- 正确答案（ground truth）

你的任务：
1. 总结system prompt的缺陷。
2. 给出一个改进后的system prompt。
3. 仅输出新的system prompt。
